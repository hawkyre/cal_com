alias CalCom.Operations

defmodule Operations.OrganizationsTeamsSchedulesControllerGetUserSchedules do
  @moduledoc "A complete typed Cal provider operation."
  use CalCom.OperationModule, source: {"operation_contracts_18.json", __MODULE__}
end

defmodule Operations.OrganizationTeamWorkflowsControllerGetWorkflows do
  @moduledoc "A complete typed Cal provider operation."
  use CalCom.OperationModule, source: {"operation_contracts_18.json", __MODULE__}
end

defmodule Operations.OrganizationTeamWorkflowsControllerCreateEventTypeWorkflow do
  @moduledoc "A complete typed Cal provider operation."
  use CalCom.OperationModule, source: {"operation_contracts_18.json", __MODULE__}
end

defmodule Operations.OrganizationTeamWorkflowsControllerGetRoutingFormWorkflows do
  @moduledoc "A complete typed Cal provider operation."
  use CalCom.OperationModule, source: {"operation_contracts_18.json", __MODULE__}
end

defmodule Operations.OrganizationTeamWorkflowsControllerCreateFormWorkflow do
  @moduledoc "A complete typed Cal provider operation."
  use CalCom.OperationModule, source: {"operation_contracts_18.json", __MODULE__}
end
