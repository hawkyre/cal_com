alias CalCom.Entities

defmodule Entities.WebhookBookingCancelledPayload do
  @moduledoc "Complete typed fields for this Cal provider object."
  use CalCom.Schema, source: {"schema_contracts_44.json", __MODULE__}
end

defmodule Entities.WebhookBookingRescheduledPayload do
  @moduledoc "Complete typed fields for this Cal provider object."
  use CalCom.Schema, source: {"schema_contracts_44.json", __MODULE__}
end

defmodule Entities.WebhookBookingRequestedPayload do
  @moduledoc "Complete typed fields for this Cal provider object."
  use CalCom.Schema, source: {"schema_contracts_44.json", __MODULE__}
end

defmodule Entities.WebhookBookingRejectedPayload do
  @moduledoc "Complete typed fields for this Cal provider object."
  use CalCom.Schema, source: {"schema_contracts_44.json", __MODULE__}
end

defmodule Entities.WebhookBookingPaidPayload do
  @moduledoc "Complete typed fields for this Cal provider object."
  use CalCom.Schema, source: {"schema_contracts_44.json", __MODULE__}
end
